const nextConfig = {
    eslint: {
        ignoreDuringBuilds: true,
    },
    output: 'export',
    basePath: '/investment-puzzles',
    images: {
        unoptimized: true,
    }
};

export default nextConfig;
